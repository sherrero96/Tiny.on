$(document).ready(
    function () {
        $("result").html(
            "<table class=\"table table-sm\">"
            + "<tbody>"
            + "<tr>"
            + "<th scope=\"row\">Numero de veces visitada</th>"
            + "<td>100</td>"
            + "</tr>"
            + "<tr>"
        )
    }
);
